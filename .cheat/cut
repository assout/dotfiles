# To cut out the third field of text or stdoutput that is delimited by a #:
cut -d# -f3

# 先頭から3文字カット
cut -c 3 hoge.txt
#
# 3-5文字カット
cut -c 3-5 hoge.txt

# 末尾文字をカット
echo hoge | head -c-3

# スペース区切りの2フィールド目を切り出す
cut -d' ' -f2

