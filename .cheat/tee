# To tee stdout to a file:
ls | tee outfile.txt

# To tee stdout and append to a file:
ls | tee -a outfile.txt

# tee - 標準出力に加えファイルにも出力する
ls | tee foo.log

# ファイルに追記
ls | tee -a foo.log

# 標準エラー出力も対象
ls 2>&1 | tee foo.log

