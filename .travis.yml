language: ruby
sudo: false
# cache: pip
# #   apt: true
addons:
  apt:
    sources:
      - dev
      - http://archive.ubuntu.com/ubuntu/
      - trusty-backports
      - restricted
      - main
      - universe
    packages:
      - devscripts
      - shellcheck
      - python-pip

# before_install:
  # - sudo add-apt-repository -y "deb http://archive.ubuntu.com/ubuntu/ trusty-backports restricted main universe"
  # - add-apt-repository -y "deb http://archive.ubuntu.com/ubuntu/ trusty-backports restricted main universe"
  # - sudo apt-get update -qq

install:
  # - pip install -r requirements.txt
  # - apt-get -qq install devscripts
  # - sudo apt-get -qq install shellcheck
  - pip install --user vim-vint
  - pip install --user bashate

script:
  - vim --cmd version --cmd quit

  - vint --version
  - vint vim/.vimrc
  - vint vim/.gvimrc
  # TODO vlmlparser通らないっぽいからダメ - vint eclipse/_vrapperrc

  - bashate -i E002,E003 .bashrc install.sh
  - shellcheck .bashrc install.sh
